{
  "name": "app-electron",
  "private": true,
  "version": "0.0.0",
  "description": "Electron platform project for 收藏管理器.",
  "main": "build/main.js",
  "scripts": {
    "build": "tsc",
    "start": "electron .",
    "pack": "npm run build && capacitor-electron vendor && electron-builder --config electron-builder.config.js",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.15.3",
    "typescript": "^5.5.0"
  }
}
